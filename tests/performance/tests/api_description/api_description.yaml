
reporting:
- module: passfail
  criteria:
    # Inbuilt Criteria
    - success of ManagementAPIDescription<${MGMT_DESC_SUCC}, ${STOP_ALIAS} as failed
    - avg-rt of ManagementAPIDescription>${MGMT_DESC_AVG_RT}, ${STOP_ALIAS} as failed

scenarios:
  ~scenario_0:
    script: api_description.jmx